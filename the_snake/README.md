# Проект. Игра «Изгиб Питона»

Вам предстоит написать на Python классическую игру «Змейка». Игру можно написать по-разному; ваше решение должно быть реализовано на принципах ООП.

▌Проект «Изгиб Питона»

Классическая компьютерная игра «Змейка» является одной из самых известных в мире игр. Простые правила, минималистичный дизайн и драйв — причины популярности этой игры на протяжении многих лет; прототип был придуман в 1976 году, а первая «Змейка», которую мы знаем, — в 1995-м.

Суть игры заключается в том, что игрок управляет змейкой, которая движется по игровому полю, разделённому на клетки.

Цель игры — увеличивать длину змейки, «съедая» появляющиеся на экране яблоки (часто изображаемые в виде точек или других символов).
## Правила игры

•  Змейка состоит из сегментов.
•  Змейка движется в одном из четырёх направлений — вверх, вниз, влево или вправо. Игрок управляет направлением движения, но змейка не может остановиться или двигаться назад.
•  Каждый раз, когда змейка съедает яблоко, она увеличивается в длину на один сегмент.
•  В классической версии игры столкновение змейки с границей игрового поля приводит к проигрышу. Однако в некоторых вариациях змейка может проходить сквозь одну стену и появляться с противоположной стороны поля. Вас будет именно так.
•  Если змейка столкнётся сама с собой — игра заканчивается.

▌Общая логика и компоненты игры

•  Игровое поле — это прямоугольник чёрного цвета размером 640 на 480 точек, разделённый на ячейки.
•  Ячейка — это квадрат размером 20 на 20 точек. Таким образом, игровое поле имеет 32 ячейки по горизонтали и 24 по вертикали.
•  У каждой ячейки есть координаты. Отсчёт координат начинается с левого верхнего угла игрового поля. Самая первая ячейка — это ячейка с координатами 0, 0. Координаты каждой ячейки определяются координатами её верхнего левого угла.
•  Змейка и Яблоко — это объекты, которые создаются и отрисовываются на игровом поле.
•  Яблоко — это просто квадрат размером в одну ячейку игрового поля. При создании яблока координаты для него определяются случайным образом и сохраняются до тех пор, пока змейка не «съест» яблоко. После этого для яблока вновь задаются случайные координаты.
•  Змейка изначально состоит из одной головы — из одной ячейки на игровом поле. Змейка постоянно куда-то ползёт и после того, как «съест» яблоко, вырастает на один сегмент. При запуске игры змейка сразу же начинает движение вправо по игровому полю.

Описать змейку проще всего списком, в котором каждый элемент – это сегмент змейки. Значение элемента списка — это координаты сегмента на игровом поле.

Координаты удобно представить через кортеж. При размере ячейки 20x20:

•  У яблока могут быть координаты, например, (40, 340).
•  Змейка, состоящая только из одной головы, может быть представлена списком [(20, 240)].
•  Если змейка состоит из двух сегментов, она может быть описана списком [(20, 240), (40, 240)].

Отрисовать яблоко на игровом поле — нарисовать квадрат, залитый цветом, размером в одну ячейку.

Отрисовать змейку — означает отрисовать столько квадратов, сколько элементов есть в списке с координатами.
Изменить положение змейки на одну ячейку — означает вставить в начало списка новый элемент с новыми координатами головы и удалить последний элемент списка.
Событие «змейка съела яблоко» — состояние, когда координаты головы змейки совпали с координатами яблока.

Код игры состоит из нескольких основных частей:

1.  Инициализация Pygame и настройка игрового окна. В начале программы настраиваются основные параметры игры, такие как размеры окна и цвет фона. Создаётся окно игры с заданными размерами, устанавливается заголовок окна.
2.  Классы игровых объектов:

    *   GameObject — это базовый класс, от которого наследуются другие игровые объекты. Он содержит общие атрибуты игровых объектов — например, атрибуты описывают позицию и цвет объекта. Этот же класс содержит и заготовку метода для отрисовки объекта на игровом поле — draw.
    *   Snake — класс, унаследованный от GameObject, описывающий змейку и её поведение. Этот класс управляет её движением, отрисовкой, а также обрабатывает действия пользователя.
    *   Apple — класс, унаследованный от GameObject, описывающий яблоко и действия с ним. Яблоко должно отображаться в случайных клетках игрового поля.
3.  Логика игры:

    *   Сначала создаются необходимые объекты.
    *   В основном цикле игры (в функции main) происходит обновление состояний объектов:
        *   Змейка обрабатывает нажатия клавиш и двигается в соответствии с выбранным направлением.
        *   Если змейка съедает яблоко, её размер увеличивается на один сегмент, а яблоко перемещается на новую случайную позицию.
        *   При столкновении змейки с самой собой игра начинается заново.
    *   Отрисовка объектов. В каждой итерации цикла змейка меняет своё положение на игровом поле на одну ячейку. По факту это означает, что в списке координат, описывающем змейку, добавляется новый элемент (новая голова в начале списка с новыми координатами) и удаляется последний. Координаты для нового элемента определяются в зависимости от направления движения.
    *   После удаления координат хвостового сегмента из списка, описывающего змейку, ячейка, с которой она «уползла», останется закрашена в цвет змейки. После отрисовки змейки по обновлённым координатам следует «затирать её след» — то есть отрисовывать чёрный (по цвету игрового поля) квадрат по координатам удалённого последнего сегмента.
    *   Обновление экрана. На каждой итерации цикла while программа вычисляет новое состояние игрового поля: определяет координаты каждого сегмента змейки и, при необходимости, координаты яблока, а также проверяет, не столкнулась ли змейка сама с собой. После выполнения всех вычислений вызывается функция pygame.display.update(), которая обновляет игровое поле на экране пользователя. Отображение игрового поля обновляется на каждой итерации цикла.

        Однако, если программа будет отрисовывать изменения игрового поля с той скоростью, на которую способен компьютер, змейка будет перемещаться по экрану столь быстро, что игрок не сможет ей управлять.

        Чтобы замедлить «течение событий», в Pygame применяется метод clock.tick(N); он изменяет выполнение кода так, что итерации цикла будут выполняться не чаще чем N раз в секунду. В прекоде метод clock.tick() вызван с аргументом 20, это означает, что положение змейки на экране будет изменяться не более 20 раз в секунду.

Игра продолжается в бесконечном цикле, пока пользователь не закроет окно.

## Классы и методы

1.  Класс GameObject:

    *   Атрибуты:
        *   position — позиция объекта на игровом поле. В данном случае она инициализируется как центральная точка экрана.
        *   body_color — цвет объекта. Он не задаётся конкретно в классе GameObject, но предполагается, что будет определён в дочерних классах.
    *   Методы:
        *   __init__ — инициализирует базовые атрибуты объекта, такие как его позиция и цвет.
        *   draw — абстрактный метод, который предназначен для переопределения в дочерних классах. Этот метод должен определять, как объект будет отрисовываться на экране. По умолчанию — pass.
2.  Класс Apple, наследуется от GameObject:

    *   Атрибуты:
        *   body_color — ц
вет яблока. В данном случае задаётся RGB-значением (красный цвет — (255, 0, 0)).
        *   position — позиция яблока на игровом поле. Яблоко появляется в случайном месте на игровом поле.
    *   Методы:
        *   __init__ — задаёт цвет яблока и вызывает метод randomize_position, чтобы установить начальную позицию яблока.
        *   randomize_position — устанавливает случайное положение яблока на игровом поле — задаёт атрибуту position новое значение. Координаты выбираются так, чтобы яблоко оказалось в пределах игрового поля.
        *   draw — отрисовывает яблоко на игровой поверхности (есть в прекоде).
3.  Класс Snake, наследуется от GameObject:

    Программа змейка — это список координат, каждый элемент списка соответствует отдельному сегменту тела змейки. Атрибуты и методы класса обеспечивают логику движения, отрисовку, обработку событий (нажата клавиша) и другие аспекты поведения змейки в игре.

    *   Атрибуты:
        *   length — длина змейки. Изначально змейка имеет длину 1.
        *   positions — список, содержащий позиции всех сегментов тела змейки. Начальная позиция – центр экрана.
        *   direction — направление движения змейки. По умолчанию змейка движется вправо.
        *   next_direction — следующее направление движения, которое будет применено после обработки нажатия клавиши. По умолчанию задать None.
        *   body_color — цвет змейки. Задаётся RGB-значением (по умолчанию — зелёный: (0, 255, 0)).
    *   Методы:
        *   __init__ — инициализирует начальное состояние змейки.
        *   update_direction — обновляет направление движения змейки (есть в прекоде).
        *   move — обновляет позицию змейки (координаты каждой секции), добавляя новую голову в начало списка positions и удаляя последний элемент, если длина змейки не увеличилась.
        *   draw — отрисовывает змейку на экране, затирая след (есть в прекоде).
        *   get_head_position — возвращает позицию головы змейки (первый элемент в списке positions).
        *   reset — сбрасывает змейку в начальное состояние после столкновения с собой.

## Функции

•   handle_keys — обрабатывает нажатия клавиш, чтобы изменить направление движения змейки (есть в прекоде).

## Основной игровой цикл

•   Создание экземпляров классов Snake и Apple до цикла.
•   В бесконечном цикле:
    *   Обрабатывайте события клавиш при помощи функции handle_keys().
    *   Обновляйте направление движения змейки при помощи метода update_direction().
    *   Двигайте змейку (модифицируйте список) при помощи метода move().
    *   Проверяйте, съела ли змейка яблоко (если да, увеличьте длину змейки и переместите яблоко).
    *   Проверяйте столкновения змейки с собой (если столкновение, сброс игры при помощи метода reset()).
    *   Отрисовывайте змейку и яблоко, используя соответствующие методы draw.
    *   Обновляйте экран при помощи метода pygame.display.update().

## Задание

•   Напишите классы GameObject, Apple и Snake, а также их атрибуты и методы.
•   Допишите основной цикл игры в функции main().
•   Всё, к чему можно написать докстринги, должно содержать докстринги.
•   Код должен соответствовать PEP 8.

Игра должна запускаться и работать, согласно правилам игры, без ошибок.
```PYTHON

# the_snake.py
# Программирование на языке высокого уровня (Python).
#
# Выполнил: Мальцев Виталий Игоревич
# Группа: ПИЖ-б-о-23-2(2)
import random
import pygame
from typing import Tuple, List, Set, Optional

# Константы для размеров поля и сетки:
SCREEN_WIDTH: int = 640  # Ширина экрана
SCREEN_HEIGHT: int = 480 # Высота экрана
GRID_SIZE: int = 20       # Размер одной клетки сетки
GRID_WIDTH: int = SCREEN_WIDTH // GRID_SIZE  # Количество клеток по ширине
GRID_HEIGHT: int = SCREEN_HEIGHT // GRID_SIZE # Количество клеток по высоте

# Направления движения:
UP: Tuple[int, int] = (0, -1)    # Вверх
DOWN: Tuple[int, int] = (0, 1)  # Вниз
LEFT: Tuple[int, int] = (-1, 0)  # Влево
RIGHT: Tuple[int, int] = (1, 0)   # Вправо

# Цвет фона - черный:
BOARD_BACKGROUND_COLOR: Tuple[int, int, int] = (0, 0, 0) # Черный цвет

# Цвет границы ячейки
BORDER_COLOR: Tuple[int, int, int] = (93, 216, 228) # Светло-голубой цвет

# Цвет яблока
APPLE_COLOR: Tuple[int, int, int] = (255, 0, 0) # Красный цвет

# Цвет змейки
SNAKE_COLOR: Tuple[int, int, int] = (0, 255, 0) # Зеленый цвет

# Скорость движения змейки:
SPEED: int = 10  # Уменьшил скорость для начала


# Классы игры
class GameObject:
    """
    Базовый класс для игровых объектов.

    Атрибуты:
        position (Tuple[int, int]): Позиция объекта на экране (x, y).
        body_color (Tuple[int, int, int], optional): Цвет тела объекта. Defaults to None.
    """

    def __init__(self, position: Tuple[int, int], body_color: Optional[Tuple[int, int, int]] = None):
        """
        Инициализирует объект игры.

        Args:
            position (Tuple[int, int]): Начальная позиция объекта.
            body_color (Tuple[int, int, int], optional): Цвет тела объекта. Defaults to None.
        """
        self.position: Tuple[int, int] = position
        self.body_color: Optional[Tuple[int, int, int]] = body_color

    def draw(self, screen: pygame.Surface) -> None:
        """
        Абстрактный метод отрисовки объекта на экране.

        Args:
            screen (pygame.Surface): Экран, на котором нужно отрисовать объект.

        Raises:
            NotImplementedError: Если метод не переопределен в дочернем классе.
        """
        raise NotImplementedError


class Apple(GameObject):
    """
    Класс яблока. Наследуется от GameObject.

    Атрибуты:
        snake_positions (List[Tuple[int, int]]): Список позиций сегментов змейки.
        position (Tuple[int, int]): Позиция яблока на экране (x, y).
        body_color (Tuple[int, int, int]): Цвет яблока.
    """

    def __init__(self, snake_positions: List[Tuple[int, int]]):
        """
        Инициализирует яблоко и устанавливает случайное положение.

        Args:
            snake_positions (List[Tuple[int, int]]): Список позиций сегментов змейки.
        """
        super().__init__((0, 0), APPLE_COLOR)  # Начальное положение, цвет яблока
        self.snake_positions: List[Tuple[int, int]] = snake_positions #Запоминаем позиции змеи, чтобы яблоко не появилось внутри неё
        self.randomize_position() #Сразу генерируем случайную позицию при создании

    def randomize_position(self) -> None:
        """Устанавливает случайное положение яблока на поле."""
        available_positions: Set[Tuple[int, int]] = set((x * GRID_SIZE, y * GRID_SIZE)
                                  for x in range(GRID_WIDTH)
                                  for y in range(GRID_HEIGHT)) - set(self.snake_positions)
        # Создаем множество всех возможных позиций на поле, а затем вычитаем из него позиции змейки.
        # Это гарантирует, что яблоко не появится внутри змейки.

        if available_positions:
            self.position: Tuple[int, int] = random.choice(tuple(available_positions)) #Выбираем случайную позицию из доступных
        else:
            # Если нет свободных мест, игра заканчивается
            print("Нет свободных мест для яблока!")
            pygame.quit() #Завершаем pygame
            raise SystemExit #Завершаем программу

    def draw(self, screen: pygame.Surface) -> None:
        """
        Отрисовывает яблоко на экране.

        Args:
            screen (pygame.Surface): Экран, на котором нужно отрисовать яблоко.
        """
        rect: pygame.Rect = pygame.Rect(self.position, (GRID_SIZE, GRID_SIZE)) #Создаем прямоугольник для яблока
        pygame.draw.rect(screen, self.body_color, rect) #Рисуем заполненный прямоугольник цветом яблока
        pygame.draw.rect(screen, BORDER_COLOR, rect, 1) #Рисуем границу прямоугольника


class Snake(GameObject):
    """
    Класс змейки. Наследуется от GameObject.

    Атрибуты:
        length (int): Длина змейки.
        positions (List[Tuple[int, int]]): Список позиций сегментов змейки.
        direction (Tuple[int, int]): Направление движения змейки.
        next_direction (Tuple[int, int], optional): Следующее направление движения (для обработки нажатий). Defaults to None.
        last (Tuple[int, int], optional): Позиция последнего удаленного сегмента (для стирания следа). Defaults to None.
    """

    DIRECTION_MAP: dict = {
        pygame.K_UP: UP,
        pygame.K_DOWN: DOWN,
        pygame.K_LEFT: LEFT,
        pygame.K_RIGHT: RIGHT,
    }
    """Словарь соответствия клавиш направлениям движения."""

    def __init__(self):
        """Инициализирует змейку."""
        start_position: Tuple[int, int] = (GRID_WIDTH // 2 * GRID_SIZE, GRID_HEIGHT // 2 * GRID_SIZE) #Начальная позиция в центре экрана
        super().__init__(start_position, SNAKE_COLOR) #Инициализируем родительский класс
        self.length: int = 1 #Начальная длина змейки
        self.positions: List[Tuple[int, int]] = [start_position] #Список позиций сегментов, начинается с головы
        self.direction: Tuple[int, int] = random.choice([UP, DOWN, LEFT, RIGHT]) #Выбираем случайное начальное направление
        self.next_direction: Optional[Tuple[int, int]] = None  # Следующее направление
        self.last: Optional[Tuple[int, int]] = None  # Последний удаленный сегмент

    def update_direction(self) -> None:
        """Обновляет направление движения."""
        if self.next_direction: #Если есть ожидающее направление
            #Проверка на движение в противоположном направлении
            if (self.next_direction[0] * -1, self.next_direction[1] * -1) == self.direction:
               return #Игнорируем смену направления, если она ведет в обратную сторону

            self.direction: Tuple[int, int] = self.next_direction #Обновляем текущее направление
            self.next_direction: Optional[Tuple[int, int]] = None #Сбрасываем ожидающее направление

    def move(self) -> None:
        """Перемещает змейку."""
        head_x: int; head_y: int
        head_x, head_y = self.positions[0] #Получаем координаты головы
        direction_x: int; direction_y: int
        direction_x, direction_y = self.direction #Получаем направление движения
        new_head: Tuple[int, int] = ((head_x + direction_x * GRID_SIZE) % SCREEN_WIDTH,
                    (head_y + direction_y * GRID_SIZE) % SCREEN_HEIGHT) #Вычисляем новые координаты головы (с учетом границ экрана)

        self.positions.insert(0, new_head) #Добавляем новую голову в начало списка
        if len(self.positions) > self.length: #Если длина змейки больше заданной
            self.last: Optional[Tuple[int, int]] = self.positions.pop()  # Удаляем последний сегмент и сохраняем его
        else:
            self.last: Optional[Tuple[int, int]] = None

    def draw(self, screen: pygame.Surface) -> None:
        """Отрисовывает змейку."""
        for position in self.positions[:-1]: #Рисуем все сегменты, кроме последнего (он будет затерт)
            rect: pygame.Rect = pygame.Rect(position, (GRID_SIZE, GRID_SIZE)) #Создаем прямоугольник для сегмента
            pygame.draw.rect(screen, self.body_color, rect) #Рисуем заполненный прямоугольник цветом змейки
            pygame.draw.rect(screen, BORDER_COLOR, rect, 1) #Рисуем границу прямоугольника

        # Отрисовка головы змейки
        head_rect: pygame.Rect = pygame.Rect(self.positions[0], (GRID_SIZE, GRID_SIZE)) #Создаем прямоугольник для головы
        pygame.draw.rect(screen, self.body_color, head_rect) #Рисуем заполненный прямоугольник цветом змейки
        pygame.draw.rect(screen, BORDER_COLOR, head_rect, 1) #Рисуем границу прямоугольника

        # Затирание последнего сегмента
        if self.last:
            last_rect: pygame.Rect = pygame.Rect(self.last, (GRID_SIZE, GRID_SIZE)) #Создаем прямоугольник для последнего сегмента
            pygame.draw.rect(screen, BOARD_BACKGROUND_COLOR, last_rect) #Рисуем заполненный прямоугольник цветом фона, чтобы стереть след

    def get_head_position(self) -> Tuple[int, int]:
        """Возвращает позицию головы змейки."""
        return self.positions[0] #Первый элемент списка - голова

    def reset(self) -> None:
        """Сбрасывает змейку в начальное состояние."""
        start_position: Tuple[int, int] = (GRID_WIDTH // 2 * GRID_SIZE, GRID_HEIGHT // 2 * GRID_SIZE) #Начальная позиция в центре экрана
        self.length: int = 1 #Сбрасываем длину
        self.positions: List[Tuple[int, int]] = [start_position] #Сбрасываем позиции
        self.direction: Tuple[int, int] = random.choice([UP, DOWN, LEFT, RIGHT]) #Выбираем случайное направление
        self.next_direction: Optional[Tuple[int, int]] = None #Сбрасываем ожидающее направление
        self.last: Optional[Tuple[int, int]] = None #Сбрасываем последний сегмент

    def handle_keys(self, event: pygame.event.Event) -> None:
        """
        Обрабатывает нажатия клавиш.

        Args:
            event (pygame.event.Event): Событие нажатия клавиши.
        """
        if event.key in self.DIRECTION_MAP: #Если нажата клавиша направления
            self.next_direction: Tuple[int, int] = self.DIRECTION_MAP[event.key] #Устанавливаем следующее направление


def main() -> None:
    """Основная функция игры."""
    pygame.init() #Инициализируем pygame

    # Настройка игрового окна:
    screen: pygame.Surface = pygame.display.set_mode((SCREEN_WIDTH, SCREEN_HEIGHT), 0, 32) #Создаем окно
    pygame.display.set_caption('Змейка') #Устанавливаем заголовок окна

    # Создание экземпляров классов:
    snake: Snake = Snake() #Создаем змейку
    apple: Apple = Apple(snake.positions) #Создаем яблоко

    # Настройка времени:
    clock: pygame.time.Clock = pygame.time.Clock() #Создаем объект для управления временем

    game_over: bool = False #Флаг окончания игры

    while not game_over: #Основной цикл игры
        clock.tick(SPEED)  # Уменьшил скорость для начала

        # Обработка событий:
        for event in pygame.event.get(): #Получаем все события
            if event.type == pygame.QUIT: #Если нажата кнопка закрытия окна
                pygame.quit() #Завершаем pygame
                raise SystemExit #Завершаем программу
            elif event.type == pygame.KEYDOWN: #Если нажата клавиша
                snake.handle_keys(event) #Передаём event в snake.handle_keys()

        # Обновление направления движения:
        snake.update_direction() #Обновляем направление движения змейки

        # Движение змейки:
        snake.move() #Перемещаем змейку

        # Проверка, съела ли змейка яблоко:
        if snake.get_head_position() == apple.position: #Если голова змейки совпадает с позицией яблока
            snake.length += 1 #Увеличиваем длину змейки
            apple = Apple(snake.positions)  # Создаем новое яблоко

        # Проверка столкновений:
        if snake.get_head_position() in snake.positions[1:]:  # Проверяем столкновение с телом
            print("Самоукус!")
            snake.reset() #Перезапускаем змейку
            apple = Apple(snake.positions)  # Создаем новое яблоко

        head_x: int; head_y: int
        head_x, head_y = snake.get_head_position()
        if not (0 <= head_x < SCREEN_WIDTH and 0 <= head_y < SCREEN_HEIGHT): #Проверяем выход за границы экрана
            print("Выход за границу!")
            snake.reset() #Перезапускаем змейку
            apple = Apple(snake.positions)  # Создаем новое яблоко

        # Отрисовка:
        screen.fill(BOARD_BACKGROUND_COLOR)  # Заполняем фон
        snake.draw(screen) #Рисуем змейку
        apple.draw(screen) #Рисуем яблоко

        # Обновление экрана:
        pygame.display.update() #Обновляем экран

    pygame.quit() #Завершаем pygame


if __name__ == '__main__':
    main() #Запускаем игру
```
